name: test
on:
  push:
    branches:
      - master
jobs:
  action:
    runs-on: ubuntu-20.04
    steps:
    - name: Checkout
      uses: actions/checkout@master
      
    - name: test
      uses: actions/github@v1.0.0
      run: |
        echo "${{ secrets.GITHUB_TOKEN }}"